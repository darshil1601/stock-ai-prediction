# AI Stock Prediction Backend — Railway API Tier
# Lightweight inference-only dependencies
# Heavy ML training/retraining handled separately (Kaggle/Colab)

# Web Framework & Server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# HTTP & Networking
requests==2.32.3
httpx==0.27.0

# Environment & Config
python-dotenv==1.0.1

# Databases & Caching
supabase==2.5.0
upstash-redis==1.1.0

# ML Inference (models loaded lazily on-demand)
tensorflow==2.16.1; python_version < "3.13"
tensorflow>=2.21,<2.22; python_version >= "3.13"

# Data Processing & Feature Engineering
scikit-learn==1.4.2; python_version < "3.13"
scikit-learn>=1.7,<1.8; python_version >= "3.13"
pandas==2.2.2; python_version < "3.13"
pandas>=2.3,<2.4; python_version >= "3.13"
numpy==1.26.4; python_version < "3.13"
numpy>=2.3,<2.4; python_version >= "3.13"

# Model/Scaler Serialization
joblib==1.4.2

# Rate Limiting
slowapi==0.1.9

# ─────────────────────────────────────────────────────────────────────────
# REMOVED (Not needed for inference-only API):
# - apscheduler          (no scheduler/background jobs)
# - transformers         (FinBERT training only, not for inference)
# - torch                (redundant when using TensorFlow)
# - safetensors          (only needed for transformers models)
# ─────────────────────────────────────────────────────────────────────────
